<div class="topic-detail {{guard.status}}">
    <h3 class="topic-heading">{{guard.topic}}</h3>
    <table class="table">
        <thead>
            <tr>
                <th class="col-md-3">Alarm</th>
                <th class="col-md-1">Status</th>
                <th class="col-md-8">Message</th>
            </tr>
        </thead>
        <tbody>
            {{#each guard.alarms as |alarm|}}
                <tr>
                    <td class="col-md-3">{{alarm.name}}</td>
                    <td class="col-md-1">
                        {{#if alarm.isOK}}
                            <span class="glyphicon glyphicon-ok"></span>
                        {{else}}
                            <span class="glyphicon glyphicon-remove"></span>
                        {{/if}}
                    </td>
                    <td class="col-md-8">{{alarm.message}}</td>
                </tr>
            {{/each}}
        </tbody>
    </table>
</div>
